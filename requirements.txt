pandas>=1.5
requests>=2.28

# Med-Flamingo Report Generator Requirements
# Install with: pip install -r requirements.txt --break-system-packages

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
Pillow>=10.0.0
pandas>=2.0.0
tqdm>=4.65.0
huggingface_hub>=0.19.0

# Optional but recommended for better performance
accelerate>=0.24.0
safetensors>=0.4.0

# Note: For GPU support, install PyTorch with CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118 --break-system-packages
